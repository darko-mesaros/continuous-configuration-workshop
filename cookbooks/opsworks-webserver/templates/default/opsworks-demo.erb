server {
        listen 80;
        listen [::]:80;

        root /var/www/opsworks-demo;
        index index.html index.htm index.nginx-debian.html;

        server_name localhost;

        location / {
                try_files $uri $uri/ =404;
        }
}
